---
phase: 05-detail-page-share
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/queries/skills.ts
  - site/src/lib/share/skill-share.ts
  - site/src/lib/share/skill-share.test.ts
autonomous: true
requirements: [SHARE-02, SHARE-05]
user_setup: []
must_haves:
  truths:
    - One shared helper resolves a single best source link using doc_url -> source_url -> github_url -> detail URL for both skill and mcp_server entries.
    - Share text generation always includes name, type label, category label, summary or excerpt, and a labeled source line.
    - Share logic can read internal source_url/source_kind on the server without widening the existing public detail API contract by default.
  artifacts:
    - site/src/lib/share/skill-share.ts
    - site/src/lib/share/skill-share.test.ts
  key_links:
    - site/src/lib/queries/skills.ts exposes a server-only share query or mapping path consumed by share text and share image generation.
---

<objective>
Create the canonical share payload layer for Phase 5.

Purpose: lock the source-link rule and text composition in one reusable server-side module before image/UI work begins.
Output: server-only share helpers, tests for degradation paths, and query-layer support for published detail records with share metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-detail-page-share/05-CONTEXT.md
@.planning/phases/05-detail-page-share/05-RESEARCH.md
@site/src/lib/queries/skills.ts
@site/src/types/skill.ts
@site/src/db/schema.ts
@site/src/db/seed-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a server-only share payload resolver</name>
  <files>site/src/lib/queries/skills.ts, site/src/lib/share/skill-share.ts</files>
  <read_first>site/src/lib/queries/skills.ts, site/src/types/skill.ts, site/src/db/schema.ts, .planning/phases/05-detail-page-share/05-CONTEXT.md</read_first>
  <action>Create a server-only share module that accepts a published detail record with access to `source_url` and `source_kind`, derives labeled source metadata, and returns a normalized share payload. Keep the existing `getSkillBySlug()` public behavior intact; add a dedicated share-oriented query/helper instead of broadening every current consumer. The resolver must implement the exact fallback order from context: `doc_url` -> `source_url` -> `github_url` -> current detail URL. Include label resolution for `official_docs`, `github_repo`, `vendor_site`, and `community_directory`.</action>
  <verify>cd site && npm run typecheck</verify>
  <acceptance_criteria>
    - `site/src/lib/share/skill-share.ts` exists and exports best-source resolution plus share-text helpers.
    - Existing public detail flow keeps returning only published items.
    - No required change is made to `shared/powerup-api-contract.ts` unless execution proves it is truly needed.
  </acceptance_criteria>
  <done>Share payload resolution is centralized and reusable by later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Encode text composition and missing-field degradation in tests</name>
  <files>site/src/lib/share/skill-share.test.ts</files>
  <read_first>site/src/db/seed-data.ts, site/src/lib/share/skill-share.ts, .planning/phases/05-detail-page-share/05-RESEARCH.md</read_first>
  <action>Add focused tests for both entry types (`skill`, `mcp_server`) covering: normal copy text generation, source-link fallback from `doc_url` to `source_url` to `github_url`, final fallback to detail URL, missing-summary fallback to description excerpt, and label mapping for source kinds. Build test fixtures directly instead of relying only on current seed records, because the seed baseline already has all three link fields populated.</action>
  <verify>cd site && npm run test</verify>
  <acceptance_criteria>
    - Tests explicitly cover at least one case where `doc_url` is missing.
    - Tests explicitly cover at least one case where both `doc_url` and `github_url` are missing.
    - Tests assert that copied text still contains a source line in all supported degradation scenarios.
  </acceptance_criteria>
  <done>Fallback behavior is locked before image or UI code depends on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
| --- | --- | --- |
| Browser -> published detail/share logic | User requests share actions from a published detail page | slug, derived share text, derived source URL |
| Query layer -> share resolver | Internal governance fields feed a derived public share payload | `source_url`, `source_kind`, `doc_url`, `github_url` |

## STRIDE Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-01 | Information Disclosure | share resolver | mitigate | Return only derived share-safe fields to the client; do not expose raw governance metadata unless execution proves it is required. |
| T-05-02 | Tampering | source-link fallback logic | mitigate | Centralize fallback logic in one helper and lock it with tests so text/image routes cannot diverge silently. |
| T-05-03 | Repudiation | degradation coverage | mitigate | Add explicit tests for missing-field scenarios so future data regressions are observable in CI. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd site && npm run test`
- [ ] `cd site && npm run typecheck`
- [ ] Share text fixtures prove the source line survives all fallback branches
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- Source-link priority is implemented exactly once and reused
- Text output covers both current happy path and future missing-link edge cases

</success_criteria>

<output>
After completion, create `.planning/phases/05-detail-page-share/05-01-SUMMARY.md`
</output>
