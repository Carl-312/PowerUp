---
phase: 05-detail-page-share
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - site/src/app/api/v1/skills/[slug]/share-image/route.ts
  - site/src/lib/share/share-image.tsx
  - site/src/lib/share/share-image-fonts.ts
  - site/src/lib/share/assets/fonts/SmileySans-Oblique-share.otf
  - site/src/lib/share/assets/fonts/LICENSE.txt
autonomous: true
requirements: [SHARE-03, SHARE-04, SHARE-05]
user_setup: []
must_haves:
  truths:
    - A published detail slug can return a generated PNG from a dedicated read-only Route Handler.
    - Missing or unpublished slugs return the same not-found boundary as current detail reads.
    - The image layout is generated from live item data, not a checked-in static image.
    - Image generation does not assume the page's `woff2` font asset is reusable; it uses a dedicated share-image font resource in a supported format and within budget.
  artifacts:
    - site/src/app/api/v1/skills/[slug]/share-image/route.ts
    - site/src/lib/share/share-image.tsx
    - site/src/lib/share/share-image-fonts.ts
    - site/src/lib/share/assets/fonts/SmileySans-Oblique-share.otf
    - site/src/lib/share/assets/fonts/LICENSE.txt
  key_links:
    - The share-image route consumes the share payload produced by Plan 01 instead of reimplementing source/data rules.
    - Share-image font loading is isolated from `site/src/app/layout.tsx` and the browser UI font pipeline.
---

<objective>
Implement deterministic server-side share image generation.

Purpose: provide a stable PNG endpoint that the detail-page UI can download or preview without relying on browser screenshotting.
Output: Route Handler, image renderer, and image-specific font/resource loading compatible with Next.js 16.2.2 `ImageResponse`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-detail-page-share/05-CONTEXT.md
@.planning/phases/05-detail-page-share/05-RESEARCH.md
@.planning/phases/05-detail-page-share/05-01-PLAN.md
@site/src/app/layout.tsx
@site/src/app/api/v1/skills/[slug]/route.ts
@site/src/lib/queries/skills.ts
@site/src/app/fonts/SmileySans-Oblique.ttf.woff2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare a share-image font asset and renderer compatible with ImageResponse</name>
  <files>site/src/lib/share/share-image.tsx, site/src/lib/share/share-image-fonts.ts, site/src/lib/share/assets/fonts/SmileySans-Oblique-share.otf, site/src/lib/share/assets/fonts/LICENSE.txt</files>
  <read_first>.planning/phases/05-detail-page-share/05-RESEARCH.md, site/src/lib/skill-presentation.ts, site/src/app/fonts/SmileySans-Oblique.ttf.woff2</read_first>
  <action>Create a dedicated poster renderer that uses flexbox-safe `ImageResponse` JSX only. The poster must include at least: name, type label, category label, summary/excerpt, and a recognizable PowerUp visual signature derived from current detail-page styling. Vendor a share-only Smiley Sans asset in supported `ttf` or `otf` format under `site/src/lib/share/assets/fonts/`, keep the upstream license text beside it, and load that asset explicitly from image-generation code rather than from `next/font/local`. Because Next.js documents a `500KB` `ImageResponse` bundle budget and the full upstream Smiley Sans files exceed it, task scope includes choosing or producing a subsetted share-font artifact and verifying its size before wiring the renderer to it. Keep the renderer data-driven so visual tokens can evolve without rewriting the route handler.</action>
  <verify>cd site && npm run typecheck</verify>
  <acceptance_criteria>
    - No DOM screenshot library is imported as the primary renderer.
    - Font loading is explicit and isolated to image-generation code.
    - The share-image font asset is not the page's existing `woff2` file.
    - The chosen share-image font artifact has a documented supported format and passes an asset-budget check before route integration.
    - Renderer JSX avoids CSS grid and other unsupported layout assumptions.
  </acceptance_criteria>
  <done>A deterministic poster renderer exists for live entry data and uses an explicit share-image font asset that is safe for `ImageResponse`.</done>
</task>

<task type="auto">
  <name>Task 2: Add a published-only share-image route</name>
  <files>site/src/app/api/v1/skills/[slug]/share-image/route.ts</files>
  <read_first>site/src/app/api/v1/skills/[slug]/route.ts, site/src/lib/share/skill-share.ts, .planning/phases/05-detail-page-share/05-CONTEXT.md</read_first>
  <action>Add `GET /api/v1/skills/[slug]/share-image` as a read-only Route Handler. Reuse the published-detail lookup and the Plan 01 share payload rather than duplicating rules inline. Return `image/png` for published entries and 404 for missing/unpublished slugs. Keep the route narrow: no templating query params, no arbitrary URL fetches, and no dynamic user-authored styling. If response headers are added, prefer same-origin friendly defaults so the client can fetch the image as a blob and control download behavior.</action>
  <verify>cd site && npm run build</verify>
  <acceptance_criteria>
    - Published slug returns PNG bytes successfully.
    - Missing or unpublished slug does not leak private content through the image route.
    - Route behavior is documented well enough for the client plan to consume without guessing headers or URL shape.
    - Route integration does not read fonts from `site/src/app/fonts/` or depend on browser CSS font variables.
  </acceptance_criteria>
  <done>The server can generate share images from live published item data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
| --- | --- | --- |
| Browser -> share-image route | User requests a generated PNG for a published slug | slug, PNG bytes |
| Route handler -> image assets | Server-side image generation loads fonts/assets from the repo | font bytes, poster config |

## STRIDE Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-04 | Information Disclosure | share-image route | mitigate | Reuse published-only lookup and return 404 for unpublished/missing slugs. |
| T-05-05 | Denial of Service | image generation path | mitigate | Keep poster layout fixed-scope, avoid unbounded remote fetches, and keep assets within the `ImageResponse` bundle budget. |
| T-05-06 | Tampering | image asset loading | mitigate | Load only repo-owned fonts/assets; do not accept user-controlled asset URLs or template ids in v1. |
| T-05-10 | Reliability | share-image font choice | mitigate | Do not rely on the page `woff2` font; use a supported-format share asset and verify its size budget before shipping. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd site && npm run build`
- [ ] `cd site && npm run typecheck`
- [ ] Share-image font asset format and size are verified against Next `ImageResponse` constraints
- [ ] Manual or scripted route check confirms published PNG + 404 behavior for missing slug
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- Share-image route is read-only, published-only, and deterministic
- Image generation uses supported Next.js 16.2.2 patterns, a supported font format, and an explicit asset-budget-safe share font

</success_criteria>

<output>
After completion, create `.planning/phases/05-detail-page-share/05-02-SUMMARY.md`
</output>
